Workspace shell migration now includes a dedicated `WorkspaceHubScreen` interface component in `modules/workspace/interfaces/components/WorkspaceHubScreen.tsx`. The `/workspace` App Router page delegates workspace-specific UI state, records rendering, and create-workspace dialog behavior to that module interface component, while the app page stays limited to shell context wiring (`useApp`) and active-account selection.