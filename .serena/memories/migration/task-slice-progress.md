Task module was advanced toward MDDD conventions: canonical repository contract now lives at modules/task/domain/repositories/TaskRepository.ts, application layer imports that contract, ports/TaskRepository.ts is a compatibility re-export, and modules/task/index.ts now exists as public API entrypoint without re-exporting server actions. TaskRepoImpl TODO DB stub was replaced with an idempotent in-memory repository implementation (instance-scoped map + safe Date cloning) to keep use-cases operational during migration.