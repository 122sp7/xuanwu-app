version: "1"
setup:
  - name: Global Setup
    description: Install Node 24 and Python uv dependencies
    steps:
      - run: npm install
      - run: cd lib/firebase/functions-python && uv sync
  - name: Verify Environment
    steps:
      - run: node -v
      - run: uv --version
