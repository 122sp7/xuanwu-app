version: "1"
setup:
  - name: Global Environment Setup
    description: Install Node 24 and Python uv
    steps:
      - run: npm install
      - run: uv sync --directory lib/firebase/functions-python
