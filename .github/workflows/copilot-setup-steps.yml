version: "1"
setup:
  - name: Auto Environment Setup
    description: Ensure both Node and Python environments are locked and synced
    steps:
      - run: npm install
      - run: |
          cd lib/firebase/functions-python
          if [ ! -f uv.lock ]; then uv lock; fi
          uv sync
