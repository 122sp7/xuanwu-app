name: Deploy Preview

on:
  workflow_dispatch:

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Scaffold note
        run: |
          echo "This workflow is a scaffold."
          echo "Add preview environment secrets and deployment target wiring before enabling automatic preview deploys."
