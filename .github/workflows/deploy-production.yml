name: Deploy Production

on:
  workflow_dispatch:

jobs:
  production:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Scaffold note
        run: |
          echo "This workflow is a scaffold."
          echo "Wire protected environments, approvals, and production deployment secrets before enabling it."
