# Copilot Hooks

This directory is intentionally created as the canonical location for Copilot hook JSON files.

- Path convention: `.github/hooks/*.json`
- Status: no active hooks yet
- Current policy: add hooks only after event contract, ownership, and rollback plan are documented.

Add hook files here only when there is an approved automation scenario and validated lifecycle event contract.
