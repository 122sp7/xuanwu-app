# Xuanwu App — Claude Entry

This file is a lightweight pointer, not a second architecture handbook.

## Read First

1. `.github/copilot-instructions.md`
2. `docs/README.md`
3. `.github/instructions/`
4. `src/modules/<context>/AGENTS.md`

## Validation

- `npm run lint`
- `npm run test`
- `npm run build`

## Rule

If guidance here conflicts with `docs/**/*` or `.github/instructions/*`, follow the owner document and update that source instead of duplicating it here.
