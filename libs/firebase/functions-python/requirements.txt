# Required platform dependencies for long-term functions-python development.
# Some libraries are for upcoming parser/vector/AI flows and are intentionally pre-pinned.
# 核心框架：建議保留版本區間，確保與 Firebase 環境相容
firebase-functions>=0.4.2,<1.0.0
firebase-admin>=6.5.0,<7.0.0

# Document AI：這部分目前版本號有衝突，可以暫時不寫，讓系統自動選
google-cloud-documentai<3.0.0
google-cloud-documentai-toolbox<1.0.0

# 其他 AI 套件：如果不寫版本，建議至少加上大版本限制 (例如 <2.0.0)
google-cloud-aiplatform<2.0.0
langchain-text-splitters<1.0.0

# 基礎雲端工具：通常很穩定，不寫版本風險較小
google-cloud-storage<3.0.0
google-cloud-firestore<3.0.0