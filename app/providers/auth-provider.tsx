"use client";

/**
 * auth-provider.tsx — backward-compat re-export shim.
 * Canonical source: modules/platform/subdomains/identity/interfaces/providers/auth-provider.tsx
 */

export { AuthProvider, useAuth } from "@/modules/platform/api";
