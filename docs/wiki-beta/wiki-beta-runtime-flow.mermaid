# Wiki-Beta Runtime and Data Flow

```mermaid
flowchart LR
  subgraph RT["Runtime Boundary"]
    direction TB
    subgraph NX["Next.js Runtime"]
      APP["app/(shell)/wiki-beta/page.tsx"]
      MOD["modules/wiki-beta<br/>interfaces/components + application use-cases"]
      DOCSUI["/wiki-beta/documents<br/>Drag & Drop Upload + Docs List + Reindex"]
      DOCSPLUS["Sidebar: Documents +<br/>Quick Create (Page / Database) (planned)"]
      APP --> MOD
      MOD --> DOCSUI
      MOD --> DOCSPLUS
    end

    subgraph FN["Firebase Functions Runtime"]
      PY["py_fn<br/>Callable handlers"]
      RAG["rag_query"]
      REIDX["rag_reindex_document"]
      PY --> RAG
      PY --> REIDX
    end

    MOD -->|"httpsCallable: rag_query(account_id, query, top_k)"| PY
    MOD -->|"httpsCallable: rag_reindex_document(account_id, doc_id, json_gcs_uri, ...)"| PY
    DOCSUI -->|"uploadBytes(file, customMetadata:{account_id, workspace_id?})"| STAPI["Firebase Storage API"]
    RAG -->|"retrieve + answer"| OUT1["RAG answer + citations"]
    REIDX -->|"re-ingest from json_gcs_uri"| OUT2["RAG status refresh"]
    DOCSUI -->|"account-wide read (default)"| OUT3["accounts/{accountId}/documents"]
    DOCSUI -->|"workspace filter (optional)"| OUT4["?workspaceId=..."]
    DOCSPLUS -->|"create page/database in account scope"| OUT5["accounts/{accountId}/pages | databases (planned)"]
    OUT1 --> MOD
    OUT2 --> MOD
  end

  subgraph NAV["Account Navigation Topology (UI)"]
    direction TB
    AC["Account"]

    subgraph PER["Personal Branch"]
      direction TB
      PERROOT["Personal"]
      PERWS["Workspaces"]
      PWSX["Workspace ..."]
      PWSN["Workspace n"]
      PKB["Knowledge Base"]
      PK1["Spaces"]
      PK2["Pages"]
      PK3["Documents (+ planned)"]
      PK4["Vector Index"]
      PK5["RAG"]
      PK6["AI Tools"]
      PERROOT --> PERWS
      PERWS --> PWSX
      PERWS --> PWSN
      PWSX --> PKB
      PWSN --> PKB
      PKB --> PK1
      PKB --> PK2
      PKB --> PK3
      PKB --> PK4
      PKB --> PK5
      PKB --> PK6
    end

    subgraph ORG["Organization Branch"]
      direction TB
      ORGROOT["Organization"]
      OM["Members"]
      OT["Teams (Permission)"]
      OWS["Workspaces"]
      OWSX["Workspace ..."]
      OWSN["Workspace n"]
      OKB["Knowledge Base"]
      OK1["Spaces"]
      OK2["Pages"]
      OK3["Documents (+ planned)"]
      OK4["Vector Index"]
      OK5["RAG"]
      OK6["AI Tools"]
      ORGROOT --> OM
      ORGROOT --> OT
      ORGROOT --> OWS
      OWS --> OWSX
      OWS --> OWSN
      OWSX --> OKB
      OWSN --> OKB
      OKB --> OK1
      OKB --> OK2
      OKB --> OK3
      OKB --> OK4
      OKB --> OK5
      OKB --> OK6
    end

    AC --> PERROOT
    AC --> ORGROOT
  end

  subgraph DM["Account Data Model (Firestore)"]
    direction TB
    A["accounts/{accountId}"]
    M["members/{userId}"]
    T["teams/{teamId}"]
    S["spaces/{spaceId}"]
    F["files/{fileId} (planned)"]
    D["documents/{documentId}"]
    P["pages/{pageId} (planned)"]
    DB["databases/{databaseId} (planned)"]
    TK["tasks/{taskId} (planned)"]
    TD["task_dependencies/{dependencyId} (planned)"]
    SK["skills/{skillId} (planned)"]
    TS["task_skill_thresholds/{thresholdId} (planned)"]
    C["chunks/{chunkId} (planned)"]
    V["vectors/{vectorId} (planned in Firestore)\nactual index: Upstash"]
    Q["rag_queries/{queryId} (planned)"]
    ANS["rag_answers/{answerId} (planned)"]

    A --> M
    A --> T
    A --> S
    A --> F
    A --> D
    A --> P
    A --> DB
    A --> TK
    A --> TD
    A --> SK
    A --> TS
    A --> C
    A --> V
    A --> Q
    A --> ANS

    F --> D
    D --> C
    C --> V
    S --> P
    DB --> TK
    TK --> TD
    SK --> TS
    TK --> TS
    S --> D
    D -. "Document to Page (manual curation)" .-> P
  end

  subgraph PIPE["RAG Ingestion + Query Pipeline"]
    direction TB
    U["Upload File (UI)"] --> ST["Storage (file)"]
    ST --> FR["Create/Init Document Index"]
    FR --> PF["Parse File"]
    PF --> CD["Update Document (parsed)"]
    CD --> CH["Chunk Document"]
    CH --> CC["Create Chunks (vector payload)"]
    CC --> EM["Embedding"]
    EM --> CV["Create Vectors (Upstash)"]
    CV --> RS["RAG Search"]
    RS --> RA["Answer"]
  end

  FR --> F
  CD --> D
  CC --> C
  CV --> V
  RS --> Q
  RA --> ANS
  Q --> RAG
  V --> RAG
```

## Migration Phases

- Phase 1: callable payload requires `account_id`.
- Phase 2: storage-trigger ingestion requires account binding (`metadata.account_id` or `uploads/{accountId}/...`).
- Phase 3 (current): Firestore reads/writes and RAG retrieval are account-scoped only.
- Phase 4 (current): `/wiki-beta/documents` reaches `/dev-tools` parity for upload/list/reindex workflow, with account-wide default list + optional workspace filter.
- Phase 5 (planned): Sidebar `Documents +` quick create for page/database (Notion-like entry).
- Phase 6 (planned): task/database classification model with dependency graph and skill-threshold gating.

## Field Summary

- accounts/{accountId}
  - name
  - type (personal | organization)
  - ownerId
- accounts/{accountId}/spaces/{spaceId}
  - name
  - description
  - visibility
- accounts/{accountId}/documents/{documentId}
  - status
  - source.{gcs_uri, filename, size_bytes, uploaded_at, mime_type}
  - parsed.{json_gcs_uri, page_count, parsed_at, extraction_ms}
  - rag.{status, chunk_count, vector_count, indexed_at, error?}
  - spaceId (optional; from upload metadata.workspace_id)
  - metadata.space_id (optional)

### Planned Collections (Not Canonical Yet)

- accounts/{accountId}/files/{fileId}
- accounts/{accountId}/chunks/{chunkId}
- accounts/{accountId}/vectors/{vectorId}
- accounts/{accountId}/pages/{pageId}
- accounts/{accountId}/databases/{databaseId}
- accounts/{accountId}/tasks/{taskId}
- accounts/{accountId}/task_dependencies/{dependencyId}
- accounts/{accountId}/skills/{skillId}
- accounts/{accountId}/task_skill_thresholds/{thresholdId}
- accounts/{accountId}/rag_queries/{queryId}
- accounts/{accountId}/rag_answers/{answerId}

## Notes

- app/(shell)/wiki-beta is the route orchestrator.
- modules/wiki-beta owns UI composition and application orchestration.
- py_fn owns callable execution for RAG query and document reindex.
- `/wiki-beta/documents` is the product entry for upload/list/reindex, aligned with `/dev-tools` workflow.
- Documents page defaults to account-wide visibility; workspace view is an explicit filter (`workspaceId`).
- Documents sidebar right-side `+` is planned for quick create (page/database) and follows account/workspace scope rules.
- For large account datasets, page/database layer is used for classification; tasks can declare dependencies and required skill thresholds.
- Namespace is a background capability (scope/slug model), not a standalone UI entry.
- wiki-beta and wiki must remain isolated by lint import boundaries.
- In topology, "Workspace ..." and "Workspace n" are illustrative for 1..n workspaces, not fixed counts.

## Runtime Boundary Contract

- Next.js Runtime owns:
  - route orchestrator and shell navigation integration
  - interfaces/components and application use-cases
  - account-scoped query orchestration and UI state
- Firebase Functions Runtime (py_fn) owns:
  - callable execution for `rag_query` and `rag_reindex_document`
  - retrieval, reindex processing, and worker-side execution details
- Firestore boundary:
  - all reads/writes are account-scoped (`accounts/{accountId}/...`)
- Module isolation boundary:
  - `wiki-beta` and `wiki` remain separate bounded contexts
  - cross-module access must go through public barrels and lint boundaries

## Allowed Interfaces (Only)

- Next.js -> py_fn:
  - `httpsCallable("rag_query")`
  - `httpsCallable("rag_reindex_document")`
- Next.js -> Storage:
  - `uploadBytes(file, metadata.customMetadata)`
- Next.js -> Firestore (read-side / UI-side):
  - account-scoped reads from `accounts/{accountId}/...`
  - optional workspace filtering through document fields (`spaceId`/`metadata.space_id`)
- py_fn -> Firestore (worker-side):
  - account-scoped writes/updates for RAG pipeline outputs

## Forbidden Crossings

- Do not move parse/chunk/embed execution into Next.js runtime.
- Do not let py_fn own browser-facing page/session/streaming UI responsibilities.
- Do not access `wiki` internals from `wiki-beta` (or inverse) via deep relative paths.
- Do not perform non-account-scoped reads/writes (no global fallback collections).

## Ownership Matrix

| Concern | Owner | Non-owner Rule |
|---|---|---|
| Route + page composition | Next.js `app/(shell)/wiki-beta` | py_fn must not render/compose pages |
| UI components + state | `modules/wiki-beta/interfaces` | py_fn must stay UI-agnostic |
| Use-case orchestration | `modules/wiki-beta/application` | keep orchestration out of page files |
| RAG callable execution | py_fn | Next.js only invokes callable |
| Reindex callable execution | py_fn | Next.js only invokes callable |
| Firestore account scope policy | both runtimes (enforced) | any non-scoped path is invalid |
| Cross-module imports | module barrels only | no `wiki-beta` <-> `wiki` deep imports |

## Implementation Checklist (Per Change)

- Confirm the change belongs to one owner in the matrix above.
- If crossing runtime boundary, use only the allowed interfaces.
- Verify account scope is explicit in payload and storage path.
- Verify `wiki-beta` boundaries are preserved by lint/import rules.
- Update this document when ownership or interfaces change.
