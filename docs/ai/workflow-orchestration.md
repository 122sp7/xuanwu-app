**描述：**
> 請針對 `@upstash/workflow` 與 `Genkit` 生成「AI 工作流編排文件」。
> * **非同步處理**：描述如何利用 QStash 處理 LLM 的長時耗任務與重試機制。
> * **階段定義**：定義一個標準的 AI Task 包含的階段：`Trigger` -> `Context Retrieval` -> `LLM Generation` -> `Human-in-the-loop (Optional)` -> `Finalize`。
> * **監控**：說明如何追蹤工作流的執行狀態與 Token 消耗。
