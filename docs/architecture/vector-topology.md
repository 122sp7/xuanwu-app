**描述：**
> 請根據專案使用的 `@upstash/vector` 生成「向量拓撲與檢索規範」。
> * **技術細節**：定義 Embedding 模型（如 Google Text Embedding）的維度、索引策略。
> * **資料結構**：描述 Metadata 的設計（如 `namespace`, `document_id`, `timestamp`），以及如何實現多租戶（Multi-tenancy）隔離。
> * **RAG 流程**：繪製從文件解析、分塊（Chunking）到向量檢索與內容召回的完整 Pipeline。

